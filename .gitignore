schemas/gschemas.compiled
# Build output: compiled from po/ by `npm run i18n:compile`.
locale/
*.zip
node_modules/
.eslintcache
.idea/
.codegraph/
# Tool-specific agent config. The tool-neutral AGENTS.md and .agents/rules are tracked.
/.claude/
/CLAUDE.md
# Local-only generator for the README brand art. Its output, docs/images/brand/*.svg,
# is committed: the README renders those through <picture>.
/.brand/
# Screen recordings. GitHub only plays <video> from its own hosts, so a demo is
# uploaded as a release/issue attachment rather than committed.
docs/images/*.mp4
