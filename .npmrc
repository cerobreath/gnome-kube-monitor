# Make the "engines" floor in package.json a hard error rather than a warning,
# so a runtime too old for the test harness fails at install instead of in a
# confusing way later. Dev tooling only; the shipped extension runs on GJS.
engine-strict=true
